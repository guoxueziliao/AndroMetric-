# 0.2.14 一致性审计（2026-05-29）

> 本文只审计 0.2.14 规划文档一致性，不代表代码已实现。

## 结论

0.2.14 当前形成执行草案 / 待实现。

已定主题：记录、复盘与训练入口收束 v1。

已定边界：

- 默认不新增 schema / migration。
- 先处理入口、文案、状态和管理动作。
- 复盘窗口必须有可感知差异。
- 训练建议、训练目标和历史记录必须分清。
- 误创建目标必须可取消、归档或隐藏。
- 报告默认表示查看，导出降为二级动作。
- 色情使用入口统一到成人行为记录主线。
- 不做补剂周期；该问题已归入 0.2.13。

## 一致范围

- `plan-0.2.14.md`：主题、边界和专题索引一致。
- `scope-and-boundaries.md`：默认不新增 schema 和明确不做一致。
- `review-window-contract.md`：7 天、14 天、30 天、周报、月报差异一致。
- `training-goal-management.md`：建议、目标、历史、取消和归档语义一致。
- `entry-and-report-semantics.md`：查看回顾和导出文件语义一致。
- `porn-use-entry-consolidation.md`：色情使用入口归属一致。
- `acceptance-and-handoff.md`：实现顺序、验收底线和停下来重谈条件一致。

## 实现前校准

- 复盘窗口入口和当前 UI 组件。
- 报告查看 / 导出入口和当前文案。
- 训练建议、训练目标列表和目标详情入口。
- 目标取消、归档、隐藏和删除的当前数据支持。
- 色情使用当前入口：日期表单、生活入口和成人行为入口。

## 不能顺手扩大

- 新增复盘算法。
- 新增训练数据模型。
- 新增 schema / migration。
- 色情内容收藏、文件保存或自动识别。
- 账号、云同步或外部 API。
