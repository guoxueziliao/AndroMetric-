# 0.2.22 一致性审计 2026-05-31

## 审计结论

0.2.22 已收口为设置与偏好整理 v1（范围档位 A），状态为执行草案 / 待实现。范围与当前代码基线一致。

## 用户确认

- 主题为设置与偏好整理 v1。
- 范围档位 A：纯信息架构整理。用户明确不做 B（组件拆分）、不做 C（偏好存储收束）。

## 与代码基线对齐（2026-05-31 实测）

- `AppSettings` 仅 8 字段、结构克制：本版不改字段，一致。
- `defaultSettings` 与字段对应：本版不改默认值，一致。
- 杂乱在 `MyView.tsx`（877 行）设置面板分区与两套标题样式：本版整理 UI，一致。
- 数据流 `AppContent` + `onUpdateSettings`：本版不动，一致。

## 与前序版本关系

- 接在 0.2.21 数据健康与自修复之后，是队列新尾。
- 与 0.2.15（导航整理）、0.2.20（视觉收尾）同为信息架构 / 收尾系；本版针对设置面板。
- 复用 0.2.20 的语义 token 做样式统一，不新增视觉。
- 不依赖 0.2.16 - 0.2.21 的产品能力，无冲突。

## 文档一致性

- `plan-0.2.22.md`：定位、范围、不做项一致。
- `scope-and-boundaries.md`：A 档边界与 B / C 暂不做一致。
- `settings-grouping-contract.md`：分区与归属与实测一致。
- `entry-and-naming.md`：标题样式与命名规则一致。
- `acceptance-and-handoff.md` / `implementation-handoff.md`：停线项一致。

## not doing 一致性

未恢复已否决方向，未越界：

- 不改字段 / 默认值 / 存储 / schema。
- 不拆组件、不收束存储（未经确认）。
- 不新增设置项或功能。
- 不引入新视觉风格。
- 不改备份 / 导出 JSON 字段名。
- 不恢复隐私模糊 / 全局搜索。

## 停线一致性

以下情况必须停下来重谈：

- 必须拆 `MyView.tsx`（升级 B）。
- 必须改字段 / 存储（升级 C / 需 schema）。
- 需要新增设置项、新功能或新视觉风格。
- 某项归属争议可能改变其功能。
