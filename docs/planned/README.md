# Active And Planned Docs Index

> 本目录只放当前开发或仍有规划维护价值的版本文档。已完成版本只保留在 `completed/README.md` 的摘要中。

## 当前状态

- 暂无 active / planned 版本。
- 历史反馈池已清理；已吸收内容以 `../completed/README.md` 和当前代码为准。

## 版本入口

- 暂无 active / planned 版本入口。

## 阅读顺序

- 看当前状态：先读本索引。
- 执行当前版本：先读 [`../roadmap/version-workbench.md`](../roadmap/version-workbench.md)。
- 做某个版本：先读对应 `plan-*.md`，再读版本目录里的 `execution-plan.md`，最后读 `implementation-handoff.md` 和专题文档。

## 维护规则

- 单篇文档尽量保持短小；过长时拆到版本目录下的专题文档。
- 0.2.20 及之后的 active / planned 版本必须有 `execution-plan.md`，作为开发会话可直接执行的任务书。
- 当前 planned 无维护中的版本。
- 旧 0.2.13 档案索引方向已删除，不恢复。
- 0.2.16 起不恢复隐私模糊，不承接全局搜索 / 快速跳转。
- 0.2.19 已完成；后续不恢复 1-100 精细权重，不改变健康结论算法。
- 0.2.20 已完成；视觉 token 系统已收尾，不恢复 `brand-*` / `pastel-*` / `palette-*` 迁移别名。
- 0.2.21 已完成；数据体检 / 修复已扩展到全表与孤立关系清理，不改健康算法，不静默删数据，不新增 schema / migration。
- 0.2.22 已完成；设置与数据面板已完成分区 / 归属 / 标题样式整理，不改字段、不动存储、不拆组件、不引入新视觉。
