# 0.2.21 一致性审计 2026-05-31

## 审计结论

0.2.21 已收口为数据健康与自修复 v1，状态为执行草案 / 待实现。范围与当前代码基线一致。

## 用户确认

- 主题为数据健康与自修复 v1。
- 经实现前代码校准，确认是「扩展现有体检 / 修复能力」，不是从零做。

## 与代码基线对齐（2026-05-31 实测）

- `runHealthCheck` 只覆盖 `logs` + `partners`：本版扩展到全表，一致。
- `repairData` 只修 `logs`：本版扩展到孤立关系清理，一致。
- `brokenRelations` 只报告不修：本版升级为可选、可撤销清理，一致。
- `handleRepairData` 已有「修复前自动快照」：本版复用，不另起无快照路径。
- `DataHealthReport` 三维 0-100 评分：本版不改算法，只扩展统计来源。

## 与前序版本关系

- 接在 0.2.20 视觉系统收尾与固化之后，是队列新尾。
- 复用 0.2.6 数据安全的快照 / 备份能力，不重做。
- 衔接 0.2.15 把「健康检查」放入数据生态区的信息架构决策。
- 不依赖 0.2.16 - 0.2.20 的产品能力，无冲突。

## 文档一致性

- `plan-0.2.21.md`：定位、范围、不做项一致。
- `scope-and-boundaries.md`：只做覆盖扩展与孤立清理一致。
- `health-check-coverage.md`：表清单与外键检查与实测一致。
- `orphan-and-relation-repair.md`：检测清单与清理策略一致。
- `repair-safety-and-backup.md`：复用快照前置一致。
- `acceptance-and-handoff.md` / `implementation-handoff.md`：停线项一致。

## not doing 一致性

未恢复任何已否决方向，未触碰禁区：

- 不改健康算法 / 评分公式 / 复盘逻辑。
- 不做医学诊断、风险评分、成瘾判定、强因果结论。
- 不静默删数据、不自动后台清理。
- 不做云端校验、账号、跨设备同步。
- 不改备份 / 导出 JSON 字段名。

## 停线一致性

以下情况必须停下来重谈：

- 需要新增 schema / migration。
- 需要改评分算法。
- 跨表关系无法从现有字段判定。
- 需要静默 / 自动清理或把孤立数据上升为健康结论。
